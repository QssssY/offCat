### TASK_10B_BACKEND_HISTORY_PAGINATION_DEFAULTS

**目标：​**  
为历史记录接口补齐和稳定分页参数支持，并设置默认分页大小：
- 模拟面试历史默认每页 5 条
- 简历诊断历史默认每页 10 条

**当前要求：​**
1. 不做面试模式相关改动
2. 不修改数据库结构
3. 只处理后端历史查询接口的分页参数支持
4. 当前端未传分页参数时，后端自动使用默认值
5. 当传入分页参数时，按前端参数优先

**接口目标：​**
- 模拟面试历史接口：
  - 默认 `pageNum = 1`
  - 默认 `pageSize = 5`
- 简历诊断历史接口：
  - 默认 `pageNum = 1`
  - 默认 `pageSize = 10`

**要求：​**
1. 检查当前历史接口 Controller 的分页参数定义
2. 如果未传参数，使用默认值
3. Service / Repository / Mapper 查询逻辑正确支持分页
4. 返回结构保持一致，不额外引入无关字段
5. 不要新增假设 DTO，不要偏离当前已回退后的项目结构

**输出要求：​**
1. 根因/现状说明
2. 修改文件清单
3. 历史接口分页参数修改点
4. 默认分页值说明
5. 修改后的关键代码