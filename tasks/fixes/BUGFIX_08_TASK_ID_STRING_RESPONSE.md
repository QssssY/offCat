# BUGFIX 08 - 简历诊断任务ID改为字符串返回

## 1. 修复目标

修复当前简历上传接口将超长 taskId 以数字形式返回给前端，导致 JavaScript 精度丢失、结果页查询失败的问题，确保上传接口返回的 taskId 以字符串形式传递，从而保证前端、路由参数、任务详情查询接口全链路使用的 taskId 完全一致。

---

## 2. 问题背景

当前简历上传接口返回示例为：

```json
{
  "code": 200,
  "message": "简历诊断任务已提交",
  "data": 2039737155583942658
}
```